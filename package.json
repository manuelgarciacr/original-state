{
    "name": "original-state",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "jest-dom": "^3.5.0",
        "react": "^16.14.0",
        "react-dom": "^16.14.0",
        "react-scripts": "^3.4.4"
    },
    "scripts": {
        "start": "react-scripts start",
        "build": "react-scripts build"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "husky": {
        "hooks": {
            "pre-commit": "lint-staged && npm run build"
        }
    },
    "browserslist": [
        ">0.2%",
        "not dead",
        "not ie <= 11",
        "not op_mini all"
    ],
    "devDependencies": {
        "husky": "^1.3.1",
        "lint-staged": "^8.2.1"
    }
}
